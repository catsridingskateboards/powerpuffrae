# powerpuffrae
i like cats and rice 

```bash 
# install git
sudo apt update
sudo apt install git
# view history of commands
history
# create an rsa private and public key
ssh-keygen
# shows you the public key you created
cat .ssh/id_rsa.pub
# create directory
mkdir project
# moving into the directory
cd project/
# downloads the repo
git clone https://github.com/catsridingskateboards/powerpuffrae.git
# list the current directories
ls
cd powerpuffrae/
# opens visual studio code
code .
```